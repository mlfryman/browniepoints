// /* jshint camelcase:false */

// 'use strict';

// var pg = require('../postgres/manager');

// MAKE FORMS & FUNCTIONS TO ADD & SUBTRACT POINTS

// function Friendship(obj){
//   this. = obj.;
// }

// Friendship.add= function(obj, cb){
//   var friendship = new Friendship(obj);

//   var psqlString = 'INSERT INTO friendships () VALUES () RETURNING id',
//       psqlParams = [];
//   pg.query(psqlString, psqlParams, function(err, results){
//     cb(err, results && results.rows ? results.rows[0] : null);
//   });
// };

// module.exports = Friendship;
